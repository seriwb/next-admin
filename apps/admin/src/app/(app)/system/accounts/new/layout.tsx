import type React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create account",
};

export default function NewAccountLayout({ children }: { children: React.ReactNode }) {
  return children;
}
